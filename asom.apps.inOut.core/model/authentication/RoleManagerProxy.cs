namespace asom.apps.inOut.core.model.authentication
{
    class RoleManagerProxy
    {
        // Ensures that the User has the required Permission to view Installed Roles
        // I Think this won't make any sense because users should be 
        //allowed to see roles assigned to them
    }
}
